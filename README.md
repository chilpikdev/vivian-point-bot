# Vivian Point Bot

Этот бот позволяет клиентам проверить свои баллы, отправив свой контакт в Telegram.

## Функциональность

- При команде `/start` бот предлагает отправить контакт
- После получения контакта проверяет, является ли пользователь клиентом
- Если клиент найден - показывает количество баллов
- Если клиент не найден - выводит сообщение "Вы не являетесь клиентом"

## Установка

### Локальный запуск

1. Клонируйте репозиторий
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` и добавьте токен вашего Telegram бота:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота_здесь
```

4. Запустите бота:

```bash
python bot.py
```

### Запуск через Docker

1. Убедитесь, что у вас установлен Docker и Docker Compose
2. Создайте файл `.env` с необходимыми переменными окружения:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота_здесь
API_BASE_URL=https://api.crm.vivian.uz
API_USERNAME=ваше_имя_пользователя
API_PASSWORD=ваш_пароль
```

3. Запустите бота с помощью Docker Compose:

```bash
docker-compose up -d
```

Для остановки контейнера используйте:

```bash
docker-compose down
```

## Настройка API

Бот интегрирован с внешним API по адресу `https://api.crm.vivian.uz/point` для проверки клиентов и получения их баллов.
Для аутентификации используются учетные данные, указанные в файле `.env`.

Конфигурация API задается в файле `.env`:
- `API_BASE_URL` - базовый URL API
- `API_USERNAME` - имя пользователя для аутентификации
- `API_PASSWORD` - пароль для аутентификации

## Использование

1. Отправьте команду `/start` боту
2. Нажмите на кнопку "📱 Отправить контакт" или отправьте контакт вручную
3. Бот проверит наличие вас в системе через API и покажет ваши баллы или сообщение об отсутствии в базе

## Режим работы

Бот использует метод `polling` для получения обновлений от Telegram API. Это означает, что бот постоянно опрашивает сервера Telegram на предмет новых сообщений.